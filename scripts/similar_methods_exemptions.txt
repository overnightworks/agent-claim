# Ledger for scripts/similar_methods.py. One "relpath:qualname — reason" per line, either
# side of an accepted pair (issue #319). Empty today: no pair in src/ or tests/ has been
# reviewed and deliberately accepted as a duplicate shape. Written by hand when one is.
tests/test_checkout.py:_bare_remote_repository_with_one_commit — a bare-remote-plus-one-commit scaffold shared with test_cli.py's _landing_repository (issue #322); that fixture owns an unrelated sibling lane's landing proof, outside this lane's claimed scope to fold together
tests/test_cli.py:_check_trunk_repository — the same bare-remote-plus-one-commit scaffold shared with test_checkout.py's _bare_remote_repository_with_one_commit (issue #322); that fixture owns an unrelated sibling lane's trunk-walk proof, outside this lane's claimed scope to fold together
tests/test_checkout.py:test_validate_slug_refuses_a_value_the_derived_rule_would_never_produce — a parametrized "refuses a malformed value" shape shared with test_board.py's test_timestamp_fails_loud_on_a_malformed_github_timestamp (issue #322); the two domains (slug shape, GitHub timestamp shape) are unrelated, so unifying them would trade domain clarity for a few shared lines
tests/test_protocol.py:test_issue_identity_requires_a_positive_integer — the same "refuses a malformed value" shape shared with test_checkout.py's test_validate_slug_refuses_a_value_the_derived_rule_would_never_produce (issue #322); the two domains (issue identity, slug shape) are unrelated, so unifying them would trade domain clarity for a few shared lines
tests/test_cli.py:test_check_json_reports_unavailable_on_a_real_forge_failure.failing_run — the generic "a fake `run` raising one forge error" closure shape shared with test_github.py's test_github_adapter_reads_an_issue_without_a_parent_as_parentless.run (issue #404); the two prove unrelated adapter behaviours (check's own envelope on a forge failure, parent_issue's own not-found read), and the review's own required rename of the enclosing test (from its obsolete-shape name) is what moved this pair past the baseline identity, not a new duplication
