# Ledger for scripts/test_budget.py. Holds only "# budget: <why> [SPEC-IDs]" lines, one per
# justified regression, added in the same change as the test growth it buys (issue #319). A
# raise with no matching line here blocks --ci. Never a count baseline: no numbers are
# recorded, and this file is never rewritten by a script -- only appended to by hand.

# budget: two distinct, non-parametrizable regressions for the scope canonicalizer -- a real-bare-remote proof that a legacy unsorted claim file still replays and releases correctly once read, and a refusal proof that the block-scope renderer no longer silently deduplicates [#331]

# budget: next's parallel:/scope unknown:/close: lines land on every action shape, so three pre-existing, intentionally separate cuttable/closeable/no-slice-row container goldens each grow the same few-line tail -- they were already one clone cluster before this change (one behavior, one assertion, apiece) and merging them into a single parametrized case would trade clarity for a handful of lines; the small parametrized-share dip is the same three functions' unchanged shape against a slightly larger suite [#348]
