# Ledger for scripts/test_budget.py. Holds only "# budget: <why> [SPEC-IDs]" lines, one per
# justified regression, added in the same change as the test growth it buys (issue #319). A
# raise with no matching line here blocks --ci. Never a count baseline: no numbers are
# recorded, and this file is never rewritten by a script -- only appended to by hand.

# budget: two distinct, non-parametrizable regressions for the scope canonicalizer -- a real-bare-remote proof that a legacy unsorted claim file still replays and releases correctly once read, and a refusal proof that the block-scope renderer no longer silently deduplicates [#331]

# budget: next's parallel:/scope unknown:/close: lines land on every action shape, so three pre-existing, intentionally separate cuttable/closeable/no-slice-row container goldens each grow the same few-line tail -- they were already one clone cluster before this change (one behavior, one assertion, apiece) and merging them into a single parametrized case would trade clarity for a handful of lines; the small parametrized-share dip is the same three functions' unchanged shape against a slightly larger suite [#348]

# budget: board estimates (issue #357) need one real end-to-end proof per storage/write shape rather than one parametrized case -- item new --size and item edit --size each get a distinct real-bare-remote state-ref proof (a fresh item's own top-level field, an existing item's field replaced without touching [record] or the body outside it) plus a separate GitHub-fake proof that the same --size edit reaches a github-stored item through the generic ForgeWriter port the whole-body item edit refuses to touch; the shapes differ enough (state-ref CAS write vs. a fake forge's PATCH) that parametrizing them over one body would hide which storage a failure belongs to, which is also why clone_lines and the parametrized-share ratio both move [#357]

# budget: the Landungen view's own trunk-walk-first, pull-request-supplement projection (issue #371) needs one real proof per storage rather than one parametrized case -- a real-bare-remote state-ref proof that two trailer-carrying trunk commits each surface their own landing row (no pull-request listing in the picture at all) and a separate GitHub-FakeForge proof that a trunk-trailer landing and an older squash pull request with no trailer both surface, deduplicated per item, the trailer path winning even when a pull request also plainly closes the same item; the two setups (a real git history vs. a faked forge's merged-PR listing) do not share an arrangement worth parametrizing over, and a third small structural test (`test_landings_derivable_and_its_not_derivable_line_are_fully_retired`) pins the retired capability flag's absence rather than duplicating existing render/JSON assertions [#371]

# budget: the deduplicated TOML string writer (issue #378 review REVISE finding 1) now escapes every control character TOML's basic-string grammar forbids, not only backslash and quote -- one standalone regression proof serializes a claim whose agent field carries a tab and a newline and reads it back with `tomllib.loads`, since there is no sibling case to parametrize this byte-identity proof against [#378]

# budget: issue #390 findings 9a and 10 each need a distinct real-git regression rather than one parametrized case -- 9a's per-candidate `git log -1` failure is proven twice, once directly against `_find_operation_id` (the unit that owns the read) and once through `push_tree`'s own retry loop (the double-push consequence the fix actually prevents, kept red against the pre-fix code before the fix landed), and 10's `merge-base --is-ancestor` exit-128 case is a separate real-bare-remote lineage proof alongside the pre-existing exit-1 rewrite proof; the three scenarios exercise different entry points and cannot share an arrangement without hiding which one a failure belongs to [#390]

# budget: `test_start_refuses_a_busy_port_naming_the_pid` (issue #388, BOARD-35) needs a real second socket actually bound and listening on the loopback port so `/proc` can name its own PID -- no parametrize table can share one live, already-bound socket, so it stays its own regression rather than a shared table row [BOARD-32, BOARD-33, BOARD-34, BOARD-35, BOARD-40, BOARD-41]

# budget: `tests/test_protect.py::test_protect_bash_cd_changes_the_directory_for_the_rest_of_the_command` -- Bash's own `cd` tracking (issue #380 delta, decision 4) needs one real end-to-end proof that a literal `cd <path> &&` -- not the payload's own `cwd` -- picks the checkout a later relative path is judged in; it necessarily shares its sibling Bash scope tests' own real-bare-remote arrangement (a linked worktree, a live claim, `_protect_main`), the same idiom every other test in this file already uses, so folding it into an existing parametrized case would hide the one thing it proves that they do not: `cd` overriding the payload's own `cwd` [#380]

# budget: `start`'s worktree/branch git plumbing (issue #322) needs one real-bare-remote proof per distinct git state -- create-from-trunk, resume-clean, refuse-dirty, refuse-branch-taken-elsewhere, refuse-wrong-branch-at-the-path, find-by-branch, remove-worktree-and-branch, and merged-ancestry-true/false -- plus one real end-to-end `aco start`/`aco release --merged` CLI proof per outcome (creates+claims, resumes, refuses-closed, removes, `--keep-worktree`, dirty-keeps, self-worktree-keeps); each exercises a different git arrangement (a fresh clone, an already-linked worktree, a dirty tree, a stray branch, a merge that has and has not happened yet) that a shared parametrized table would either hide behind a builder callable or force into an unrelated shape, so parametrized_share_pct dips slightly against the larger suite [#322]

# budget: a real-bare-remote proof that `start`'s worktree creation fetches and builds from the remote trunk, and a second that a clean second call only resumes and reclaims that same worktree rather than rebuilding it (issue #322); a fresh clone and an already-linked worktree are different git arrangements, not a shared parametrized table [#322]

# budget: two more real-bare-remote refusals for `start`'s own worktree resolution -- a dirty resume, and a branch name already taken by no worktree of this item (issue #322); a dirty tree and a stray branch are unrelated git states, not worth a shared parametrized table [#322]

# budget: a real-bare-remote proof that a worktree sitting on the wrong branch at the computed path is refused, and a separate proof that `release`'s own by-branch worktree lookup finds the one matching path among several (issue #322); one is a `start`-side refusal, the other a `release`-side lookup, not a shared shape [#322]

# budget: a real-bare-remote proof that a landed worktree and its local branch are both removed, and a separate proof that merged-ancestry reads false before a real merge and true only after one (issue #322); a removal and an ancestry check exercise different git plumbing, not a shared parametrized table [#322]

# budget: one real end-to-end `aco start` CLI proof that a fresh call creates the worktree, the branch, and the claim together, and a second that a clean second call only resumes it (issue #322); a fresh build and a resume are different arrangements, not a shared parametrized table [#322]

# budget: one real end-to-end proof that `aco start` refuses a closed item before any worktree, and a separate real end-to-end proof that `aco release --merged` removes a clean, merged lane's worktree and branch (issue #322); a pre-write refusal and a post-merge cleanup are unrelated CLI paths, not a shared parametrized table [#322]

# budget: one parametrized real end-to-end `aco release --merged` proof covers every reason the worktree/branch cleanup keeps rather than removes -- `--keep-worktree`, a dirty tree, running from inside the worktree itself, no linked worktree found, an unmerged branch, a branch checked out elsewhere, and a git failure resolving the worktree (issue #322); every case differs only in which precondition fails, not in the CLI path it exercises, so one shared table is the right shape [#322]
